/*
array : same data type
continuos
*/
#include <iostream>

#include <bits/stdc++.h>

using namespace std;

const long long m = 10000000;

int arr[m]; // max length globally

int main()
{

    long long n = 1000000;
    int arr[n]; // max length in funtion

    // 0 based indexing

    return 0;
}